# Proyecto Empresa

Este proyecto simula un sistema básico de gestión empresarial en Python.

## 📁 Estructura
- `src/`: Contiene el código fuente del sistema.
  - `app.py`: Punto de entrada del programa.
  - `login.py`: Módulo para autenticación de usuarios.
  - `orden_compra.py`: Gestión de órdenes de compra.
  - `menu.py`: Menú principal que conecta los módulos.
- `database/`: Contiene la base de datos del proyecto (`proyecto.db`).
- `evidencias/`: Carpeta donde se almacenan capturas del repositorio GitHub (ramas, commits, merges, tablero Kanban).

## 🚀 Ejecución
Para ejecutar el proyecto:
```bash
cd src
python app.py
